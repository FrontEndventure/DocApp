import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ChatItem = () => {
  return (
    <View>
      <Text>ChatItem components</Text>
    </View>
  );
};

export default ChatItem;

const styles = StyleSheet.create({});
